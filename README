## Simulator Instructions
 
To run the sim in its current state:
	`cd pipe`i
	`mkfifo sim_in, camera_f camera_d` 
	Open Blender. Go to File->User Preferences->Scripts, and enter the full path to marlin/python (e.g. `/home/$USER/marlin/python`)  
